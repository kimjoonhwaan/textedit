# Markdown Editor

로컬에서 동작하는 간단한 옵시디언 스타일 Markdown 에디터 예제입니다.

## 주요 기능
- Markdown(.md) 파일 생성/저장
- 파일 목록 보기
- 파일 내용/이름 검색

## 실행 방법
1. 의존성 설치: `npm install`
2. 서버 실행: `npm start`
3. 브라우저에서 `http://localhost:3000` 접속
